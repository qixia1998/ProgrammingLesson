#include <stdio.h>  // 包含文件，之所以使用printf，和这行代码有关

int main() {        // 主函数部分，整个程序的入口位置
    printf("hello my friend!");  // 打印函数
    printf("Hi, my friend:\n\tthis is the first day I know you.");
    return 0;
}